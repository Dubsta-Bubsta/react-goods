let initialState = {  
    
}

const compareReducer = (state = initialState, action) => {
    switch (action.type) {
        
       
        default:
            return state;
    }
}

export default compareReducer;